spring:
  application.name: stores
  datasource:
    url: jdbc:h2:file:./stores/src/main/resources/db/store_bd
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2
server:
  port: 8082
